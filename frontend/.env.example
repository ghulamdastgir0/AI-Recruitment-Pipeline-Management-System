# NestJS backend base URL (REST). Also used to derive the WebSocket URL
# for the AI interview (same host, /interviews namespace).
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000"

# Static company name shown on the candidate apply page — this is a
# single-tenant system (no Company entity in the DB).
NEXT_PUBLIC_COMPANY_NAME="AI Recruitment Pipeline"
